{
  "name": "omni-publisher-skill",
  "version": "1.1.0",
  "description": "Premium Skill Package for omni-publisher",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.js",
    "install-skill": "npm run validate && gemini skills install .",
    "package": "npm run validate && zip -r omni-publisher.skill . -x '.*' '__MACOSX' '.git/*'"
  }
}
